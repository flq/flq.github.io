---
interface Props {
  id: string;
  file?: string;
}

const { id, file } = Astro.props;
const query = file ? new URLSearchParams({ file }) : "";
---

<script
  cross-origin="anonymous"
  src={`https://gist.github.com/flq/${id}.js${
    query ? "?" + query.toString() : ""
  }`}
></script>
